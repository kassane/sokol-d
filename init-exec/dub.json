{
	"name": "init-exec",
	"description": "A minimal sokol-d application.",
	"dependencies": {
		"sokol-d": "~>0.2.1"
	},
	"targetPath": "build",
	"targetType": "executable",
	"environments": {
		"SOKOL_ROOTPATH": "${SOKOL_D_PACKAGE_DIR}"
	},
	"configurations": [
		{
			"name": "native",
			"subConfigurations": {
				"sokol-d": "sokol-static"
			}
		},
		{
			"name": "wasm",
			"targetType": "staticLibrary",
			"postBuildCommands": [
				"$DC -run ${SOKOL_D_PACKAGE_DIR}/build.d -- --target=wasm --backend=gles3 --link=blend --verbose",
				"$DC -run ${SOKOL_D_PACKAGE_DIR}/build.d -- --run=blend --verbose"
			],
			"targetName": "blend",
			"subConfigurations": {
				"sokol-d": "sokol-wgles3"
			}
		},
		{
			"name": "wgpu",
			"targetType": "staticLibrary",
			"postBuildCommands": [
				"$DC -run ${SOKOL_D_PACKAGE_DIR}/build.d -- --target=wasm --backend=wgpu --link=blend --verbose",
				"$DC -run ${SOKOL_D_PACKAGE_DIR}/build.d -- --run=blend --verbose"
			],
			"targetName": "blend",
			"subConfigurations": {
				"sokol-d": "sokol-wgpu"
			}
		}
	]
}