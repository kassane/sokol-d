{
    "name": "instancingcompute",
    "sourceFiles": [
        "../shaders/instancingcompute.d"
    ],
    "dependencies": {
        "sokol-d": {
            "path": "../.."
        }
    },
    "environments": {
        "SOKOL_ROOTPATH": "${SOKOL_D_PACKAGE_DIR}"
    },
    "targetPath": "build",
    "targetName": "instancingcompute",
    "configurations": [
        {
            "name": "native",
            "targetType": "executable",
            "subConfigurations": {
                "sokol-d": "sokol-static"
            }
        },
        {
            "name": "wasm",
            "targetType": "staticLibrary",
            "postGenerateCommands": [
                "$DC -run ${SOKOL_D_PACKAGE_DIR}/build.d -- --target=wasm --backend=gles3 --link=instancingcompute --verbose"
            ],
            "subConfigurations": {
                "sokol-d": "sokol-wgles3"
            }
        },
        {
            "name": "wgpu",
            "targetType": "staticLibrary",
            "postGenerateCommands": [
                "$DC -run ${SOKOL_D_PACKAGE_DIR}/build.d -- --target=wasm --backend=wgpu --link=instancingcompute --verbose"
            ],
            "subConfigurations": {
                "sokol-d": "sokol-wgpu"
            }
        }
    ]
}